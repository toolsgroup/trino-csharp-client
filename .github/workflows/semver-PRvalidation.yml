name: Semantic Version PR Label validation

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:

  Check-Required-Labels:
    uses: toolsgroup/tg-shared-actions/.github/workflows/shared-semver-labelcheck.yml@main
    secrets: inherit
